.content_container
    padding 1rem
    min-height inherit

.frontmatter_info
    padding 1rem
    text-align center
    // border-bottom 1px solid var(--divider-color)

.frontmatter-title
    letter-spacing -.02em
    line-height 2.5rem
    font-size 2rem
    margin-bottom 2rem

.frontmatter-date
    margin-bottom 1rem

.frontmatter_tags
    position inherit
    display flex
    align-items center

